const registerMusisi = async (req, res) => {
  // Implementasi logika bisnis untuk register musisi
};

const uploadRecording = async (req, res) => {
  // Implementasi logika bisnis untuk mengupload rekaman musik atau video
};

module.exports = {
  registerMusisi,
  uploadRecording,
};
