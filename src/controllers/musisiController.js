const registerMusisi = async (req, res) => {
  // Implementasi logika untuk register musisi
};

const uploadRecording = async (req, res) => {
  // Implementasi logika untuk mengupload rekaman musik atau video
};

module.exports = {
  registerMusisi,
  uploadRecording,
};
